import { Routes } from '@angular/router';

import { CommercesTableComponent } from './commerces-table/commerces-table.component';

export const routes: Routes = [
    { path: '', component: CommercesTableComponent }
];

